import ProfileContent from './ProfileContent';
import { Stack } from '@mui/material';

const ProfilePage = () => {
  return (
    <Stack sx={{ flexGrow: 1 }}>
      <ProfileContent />
    </Stack>
  );
};

export default ProfilePage;
